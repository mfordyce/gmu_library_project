
<h1>All Users</h1>
<p>Available as of <%= formatted_time(@available_at) %></p>
<%= page_entries_info @users %>
<ol>

<% @users.each do |user| %>
  <li><%= link_to(user.name, user) %></li>
  <p>&nbsp;</p>
<% end %>
</ol>
<%= paginate @users %>
