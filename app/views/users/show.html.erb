

<% content_for :name, @user.name %>


<article class="name">
  <header>
    <h2><%= @user.name %></h2>
  </header>
  <p><label>User ID number:</label> <%= @user.id %></p>
  <p><label>Admin:</label> <%= @user.admin %></p>

</article><%= link_to 'All Users', users_path %>
| <%= link_to 'Edit', edit_user_path(@user) %>

<%= button_to 'Delete', @user,
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
